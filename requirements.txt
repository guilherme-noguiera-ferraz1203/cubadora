# Dependências comuns (PC e Pi). Mantidas mínimas para a demo rodar fácil.
PyYAML>=6.0        # leitura de config.yaml (opcional para a demo, usada em produção)

# Adicionadas conforme as fases avançam:
# requests>=2.31   # integração REST (ERP/nuvem) - Fase 8
# fastapi>=0.110   # painel web - Fase 9
# uvicorn>=0.29
# pymodbus>=3.6    # Modbus RTU (CLP/balança dinâmica) - Fase 4
